HOMEWORK 5

1.OBJETOS
Un "objeto" es un conjunto de caracteristica, que pueden agruparse con el nombre de lo que describen.
Por ejemplo, el objeto "perro" agrupa las caracteristicas pelaje, color, altura, peso, etc, de algún perro.


2.PROPIEDADES
Las "propiedades" son caracteristicas o cualidades de un objeto, que nos ayudan a conocerlo mejor. Es decir,
si quiero describir una casa, podria tener en cuenta el color exterior, el color interior, la altura, la cantidad de habitaciones, etc,
que tendrán distintos valores según el objeto. 

3.METODO
Un metodo es una funcion que representa una caracteristica del objeto. De manera sencilla, si hablamos de una persona
por ejemplo (el objeto), una caracteristica de esa persona que puede cambiar es el humor del dia (la funcion), y puede resultar
en feliz, enojado, triste, etc, según algunas condiciones del dia (si hace calor, o frio, si es lunes, si es viernes, etc).

4.BUCLE FOR...IN 
Un "bucle for...in" es una forma de recorrer las caracteristicas del objeto una a una, en orden, de modo que puedo
conocerlas y conocer los valores que van tomando las mismas.

5.NOTACION DE PUNTOS VS NOTACION DE CORCHETES
La notacion de puntos es una forma mas facil de concoer o dar valores a una caracteristica del objeto.
Es decir, conociendo como se ordenan las caracteristicas del objeto, puedo darle valores a algunas, o 
añadir otras nuevas. Para ello, vamos colocando un punto cada vez que nos adentramos en el objeto.
Por ejemplo: perro.pelaje = marron (asigno valor)

La notacion de corchetes, si bien tiene el mismo uso que anteriormente,podemos usarla cuando creamos objetos 
mas complejos.
Por ejemplo, si queremos describir a los maestros de una escuela. El arreglo sera "maestros", y dentro de 
él tendremos muchos objetos, uno por cada maestro. Y cada maestro, tendrá sus propias caracteristicas.
Con la notacion en corchetes, podemos ir asignado valores o conociendo los valores,

